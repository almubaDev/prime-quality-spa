import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Prime Quality SpA - Inspección Profesional de Frutas",
  description: "Más de 10 años de experiencia en control de calidad de frutas de exportación. Servicios profesionales para cítricos, paltas, uva, pomáceas y arándanos.",
  icons: {
    icon: '/images/IMG_Logos/PNG/imagotipo azul PQ@4x.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
